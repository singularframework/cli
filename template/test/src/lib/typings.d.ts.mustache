import { LogParser } from './log-parser';
import { ChildProcess } from 'child_process';
import { Got } from 'got';

// Global typings
declare global {

  /**
  * Listens to server logs and parses them as objects.
  * Events: 'all', 'debug', 'info', 'notice', 'warn', and 'error'.
  */
  const logs: LogParser;
  /** The running server's process. */
  const server: ChildProcess;
  /** Pre-configured Got instance for sending requests to the server. */
  const request: Got;

}
